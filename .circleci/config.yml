version: 2.1

jobs:
  build:
    docker:

      - image: cimg/python:3.9

    steps:

      - checkout


      - run:
          name: Install Dependencies
          command: |
            pip install requests pytest



      - run:
          name: Run Tests
          command: |
            pytest
